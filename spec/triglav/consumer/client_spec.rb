require "spec_helper"

describe Triglav::Consumer::Client do
  it "has a version number" do
    expect(Triglav::Consumer::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
